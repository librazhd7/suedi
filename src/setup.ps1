# fetch markdowns from this repo,
# and translate them to htmls for later parsing of urls containing the desired .jars.
# extract them to desired path after checking the validity of desired loader,
# but in an isolated and non-conflicting way of already pre-existing instances.
